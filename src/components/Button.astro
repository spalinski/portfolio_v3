---
const { href } = Astro.props;
---

<!-- <div class="mt-4">
  <a href={href}>
    <button
      type="button"
      class="font-raleway text-primary-50 bg-secondary-900 hover:bg-secondary-500 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mt-4 mb-2 dark:bg-blue-800 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800"
    >
      <slot />
    </button>
  </a>
</div> -->
<div
  class="bg-[url('/logo_cropped.png')] bg-no-repeat bg-contain bg-center p-6 flex items-center justify-center"
>
  <div class="p-4 flex items-center justify-center">
    <a
      href={href}
      target="_blank"
      class="block no-underline hover:underline text-xl"
    >
      <slot />
    </a>
  </div>
</div>
